import { FC, PropsWithChildren } from 'react';
import styles from './styles.less';

const ListLayout: FC<PropsWithChildren> = () => {
  return <div className={styles.listLayout}></div>;
};

export default ListLayout;
